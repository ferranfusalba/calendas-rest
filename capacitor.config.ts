import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'calendas-rest',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
